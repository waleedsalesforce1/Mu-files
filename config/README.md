# config/

Place configuration files here.

**Examples:**
- `settings.json` – application settings
- `app.yaml` – service configuration
- `.env.example` – environment variable template (never commit the real `.env`)
